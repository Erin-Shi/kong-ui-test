// Simple helper to collect allure results directory after each test run
// Tests will need to produce allure-results artifacts (via cypress-allure-plugin or manual writes)

// This file left intentionally minimal. If you use cypress-allure-plugin, import and setup it here.

// Example (if cypress-allure-plugin installed):
// import '@shelex/cypress-allure-plugin';

