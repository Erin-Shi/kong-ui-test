module.exports = (on, config) => {
  // If you need to write custom node event handlers for Cypress, add them here.
  // This project currently does not need extra handlers for Allure or junit.
  return config
}
